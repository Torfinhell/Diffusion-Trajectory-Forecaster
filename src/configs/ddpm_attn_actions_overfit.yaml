defaults:
  - model: diffusion_attn_actions_overfit
  - dataloaders: overfit
  - feat_extract: small_no_scenes
  - transforms: null
  - logger: clearml
  - metrics: all_metrics
  - visual: default
  - diffusion_sampler: ddpm
  - _self_

trainer:
  train_mode: debug
  num_epochs: 20
  train_epoch_len: 1.0
  val_epoch_len: 1.0
  gradient_clip_val: 5
  log_every_n_steps: 1
  seed: 1
  log_validation: true
  inner_steps: 1
  train_metric_batches: 1
  val_metric_batches: 1
  train_metric_every_n_epochs: 50
  val_metric_every_n_epochs: 10
  check_val_every_n_epoch: 1
  samples_per_scene: 1
  generate_every_epoch: 0
  print_hparams: False
  logging: "online"
  load_best_checkpoint: False
  enable_profiler: False
  enable_jax_profiler: False
  jax_profiler_dir: null
  jax_profiler_start_step: 2
  jax_profiler_num_steps: 3
  proxy_val_loss:
    enabled: false
    step_stride: 5
    include_last: true

visual:
  enable_visualization: false

loss:
  _target_: src.losses.MSELoss
  accel_scale: 1.0
  yaw_rate_scale: 0.15

optimizer:
  _target_: optax.adam

scheduler:
  _target_: optax.cosine_decay_schedule
  init_value: 5e-3
  decay_steps: 650
  alpha: 0.01
